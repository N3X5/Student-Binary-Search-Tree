#ifndef DEFINITIONS_H
#define DEFINITIONS_H

// A header, which includes definitions used by main() for printing command results

#define RECORD_NOT_FOUND "Record not found!"
#define RECORD_INSERTED "Record inserted!"
#define RECORD_DELETED "Record deleted!"

#endif